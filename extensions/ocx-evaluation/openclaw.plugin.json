{
  "id": "ocx-evaluation",
  "name": "Evaluation",
  "description": "Async evaluation system for task quality scoring, model comparison, and tool intelligence analysis.",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "pollIntervalMs": {
        "type": "number",
        "minimum": 1000,
        "default": 30000,
        "description": "How often to check for unscored runs (ms)"
      },
      "defaultJudgeModel": {
        "type": "string",
        "default": "gpt-4.1-mini",
        "description": "Default LLM model for judge scoring"
      },
      "batchSize": {
        "type": "number",
        "minimum": 1,
        "maximum": 100,
        "default": 10,
        "description": "Maximum runs to evaluate per poll cycle"
      },
      "qualityRiskThreshold": {
        "type": "number",
        "minimum": 0,
        "maximum": 100,
        "default": 40,
        "description": "Score threshold for quality-risk escalation"
      },
      "scorecardRetentionDays": {
        "type": "number",
        "minimum": 1,
        "default": 90,
        "description": "Retention period for scorecards in days"
      },
      "judgeProfilesFile": {
        "type": "string",
        "default": "judge-profiles.json",
        "description": "Filename for judge profiles storage"
      }
    }
  },
  "uiHints": {
    "pollIntervalMs": {
      "label": "Poll Interval (ms)",
      "help": "How often the evaluation worker checks for unscored runs",
      "advanced": true
    },
    "defaultJudgeModel": {
      "label": "Default Judge Model",
      "help": "LLM model used for judge-based scoring when no judge profile specifies one"
    },
    "batchSize": {
      "label": "Batch Size",
      "help": "Maximum number of runs to evaluate per poll cycle",
      "advanced": true
    },
    "qualityRiskThreshold": {
      "label": "Quality Risk Threshold",
      "help": "Scores below this value trigger quality-risk events (0-100)"
    },
    "scorecardRetentionDays": {
      "label": "Scorecard Retention (days)",
      "help": "How long to retain scorecards before cleanup"
    },
    "judgeProfilesFile": {
      "label": "Judge Profiles File",
      "help": "Filename (relative to state dir) for judge profile storage",
      "advanced": true
    }
  }
}
